# Retail Customer Insights SQL Analysis

## Project Overview

This project analyzes retail customer shopping behavior using Python, PostgreSQL, pgAdmin 4 and SQL.

The dataset was cleaned and prepared in Python, then imported into a PostgreSQL database for business analysis.

The goal of this project is to extract actionable insights about customer revenue, purchasing behavior, promotions, subscriptions, age groups and product performance.

---

## Tools Used

- Python
- Pandas
- PostgreSQL
- pgAdmin 4
- SQL
- Jupyter Notebook
- SQLAlchemy
- psycopg2

---

## Dataset Description

The dataset contains retail customer shopping behavior information.

Main columns include:

- Customer ID
- Age
- Gender
- Item Purchased
- Category
- Purchase Amount (USD)
- Location
- Size
- Color
- Season
- Review Rating
- Subscription Status
- Shipping Type
- Discount Applied
- Promo Code Used
- Previous Purchases
- Payment Method
- Frequency of Purchases
- Age Group
- Frequency Days

---

## Data Preparation

Before running the SQL analysis, the dataset was cleaned and transformed using Python.

Main preparation steps included:

- Cleaning and standardizing column names
- Handling missing values
- Creating an age group column
- Mapping purchase frequency into number of days
- Importing the cleaned dataset into PostgreSQL

The final PostgreSQL table used for analysis is:

```sql
public.customers
PostgreSQL Import Script

The project includes a Python script named:

import_to_postgresql.py

This script:

Automatically detects a CSV file in the project folder
Loads the dataset with pandas
Connects to PostgreSQL using environment variables
Imports all rows and columns into the public.customers table
Verifies the number of imported rows and columns
Environment Variables

Before running the import script, set the following environment variables:

POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=postgres

These variables are used to connect securely to PostgreSQL without exposing credentials in the source code.

Business Questions

This project answers the following business questions:

What is the revenue generated by gender?
Which customers used a promo code and spent more than the average purchase amount?
What are the top 3 products with the highest average customer satisfaction?
Which product categories generate the highest revenue?
What is the average purchase amount by gender?
Which age group generates the highest revenue?
Do subscribed customers spend more than non-subscribed customers?
What is the revenue impact of promo codes?
How can customers be segmented based on purchase value and frequency?
How can products be segmented based on customer satisfaction and revenue?
SQL Analyses
1. Revenue by Gender
SELECT 
    "Gender",
    SUM("Purchase Amount (USD)") AS total_revenue
FROM public.customers
GROUP BY "Gender";
2. Promo Code Users Above Average Purchase
SELECT 
    "Customer ID",
    "Gender",
    "Age",
    "Item Purchased",
    "Category",
    "Purchase Amount (USD)",
    "Promo Code Used",
    (SELECT AVG("Purchase Amount (USD)") FROM public.customers) AS average_purchase
FROM public.customers
WHERE "Promo Code Used" = 'Yes'
AND "Purchase Amount (USD)" > (
    SELECT AVG("Purchase Amount (USD)")
    FROM public.customers
)
ORDER BY "Purchase Amount (USD)" DESC
LIMIT 30;
3. Top 3 Products by Average Review Rating
SELECT 
    "Item Purchased",
    AVG("Review Rating") AS average_rating
FROM public.customers
GROUP BY "Item Purchased"
ORDER BY average_rating DESC
LIMIT 3;
4. Revenue by Product Category
SELECT 
    "Category",
    SUM("Purchase Amount (USD)") AS total_revenue
FROM public.customers
GROUP BY "Category"
ORDER BY total_revenue DESC;
5. Average Purchase Amount by Gender
SELECT 
    "Gender",
    ROUND(AVG("Purchase Amount (USD)")::numeric, 2) AS average_purchase
FROM public.customers
GROUP BY "Gender";
6. Revenue by Age Group
SELECT 
    "age_group",
    COUNT(*) AS number_of_customers,
    SUM("Purchase Amount (USD)") AS total_revenue,
    ROUND(AVG("Purchase Amount (USD)")::numeric, 2) AS average_purchase
FROM public.customers
GROUP BY "age_group"
ORDER BY total_revenue DESC;
7. Spending Comparison by Subscription Status
SELECT 
    "Subscription Status",
    COUNT(*) AS number_of_customers,
    SUM("Purchase Amount (USD)") AS total_revenue,
    ROUND(AVG("Purchase Amount (USD)")::numeric, 2) AS average_purchase
FROM public.customers
GROUP BY "Subscription Status"
ORDER BY average_purchase DESC;
8. Revenue Comparison With and Without Promo Code
SELECT 
    "Promo Code Used",
    COUNT(*) AS number_of_orders,
    SUM("Purchase Amount (USD)") AS total_revenue,
    ROUND(AVG("Purchase Amount (USD)")::numeric, 2) AS average_purchase
FROM public.customers
GROUP BY "Promo Code Used"
ORDER BY total_revenue DESC;
9. Customer Segmentation by Purchase Value and Frequency
SELECT 
    "Customer ID",
    COUNT(*) AS number_of_orders,
    SUM("Purchase Amount (USD)") AS total_spent,
    ROUND(AVG("Purchase Amount (USD)")::numeric, 2) AS average_purchase,
    AVG("Frequency Days") AS average_frequency_days,
    CASE 
        WHEN SUM("Purchase Amount (USD)") >= (
            SELECT AVG(total_customer_spent)
            FROM (
                SELECT SUM("Purchase Amount (USD)") AS total_customer_spent
                FROM public.customers
                GROUP BY "Customer ID"
            ) sub
        )
        AND COUNT(*) >= 2
        THEN 'High Value Customer'

        WHEN SUM("Purchase Amount (USD)") < (
            SELECT AVG(total_customer_spent)
            FROM (
                SELECT SUM("Purchase Amount (USD)") AS total_customer_spent
                FROM public.customers
                GROUP BY "Customer ID"
            ) sub
        )
        THEN 'Low Value Customer'

        ELSE 'Medium Value Customer'
    END AS customer_segment
FROM public.customers
GROUP BY "Customer ID"
ORDER BY total_spent DESC;
10. Product Segmentation by Satisfaction and Revenue
WITH product_performance AS (
    SELECT 
        "Item Purchased",
        "Category",
        COUNT(*) AS number_of_orders,
        SUM("Purchase Amount (USD)") AS total_revenue,
        ROUND(AVG("Purchase Amount (USD)")::numeric, 2) AS average_purchase,
        ROUND(AVG("Review Rating")::numeric, 2) AS average_rating
    FROM public.customers
    GROUP BY 
        "Item Purchased",
        "Category"
),
averages AS (
    SELECT
        AVG(total_revenue) AS avg_revenue,
        AVG(average_rating) AS avg_rating
    FROM product_performance
)
SELECT 
    p."Item Purchased",
    p."Category",
    p.number_of_orders,
    p.total_revenue,
    p.average_purchase,
    p.average_rating,
    CASE
        WHEN p.average_rating >= a.avg_rating 
             AND p.total_revenue < a.avg_revenue
        THEN 'High Potential Product'
        WHEN p.average_rating >= a.avg_rating 
             AND p.total_revenue >= a.avg_revenue
        THEN 'Star Product'
        WHEN p.average_rating < a.avg_rating 
             AND p.total_revenue >= a.avg_revenue
        THEN 'Revenue Driver but Low Satisfaction'
        ELSE 'Low Priority Product'
    END AS product_segment
FROM product_performance p
CROSS JOIN averages a
ORDER BY 
    product_segment,
    p.average_rating DESC,
    p.total_revenue DESC;
Key Insights

This analysis helps identify:

Which customer groups generate the most revenue
How promo codes impact customer spending
Which age groups are the most profitable
Whether subscribed customers spend more than non-subscribed customers
Which products have the highest customer satisfaction
Which product categories generate the most revenue
Which customers can be considered high-value customers
Which products have strong satisfaction but lower revenue potential
Business Value

The insights from this project can support:

Marketing campaign optimization
Customer segmentation
Product promotion strategy
Revenue growth analysis
Loyalty and subscription strategy
Product performance monitoring
Data-driven business decision-making
Project Structure
retail-customer-insights-sql/
│
├── README.md
├── sql_queries.sql
├── import_to_postgresql.py
├── requirements.txt
└── dataset.csv
How to Run This Project
Clone this repository.
git clone https://github.com/your-username/retail-customer-insights-sql.git
Install the required Python packages.
pip install -r requirements.txt
Set PostgreSQL environment variables.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=postgres
Run the import script.
python import_to_postgresql.py
Open pgAdmin 4 and run the SQL queries from:
sql_queries.sql
Conclusion

This project demonstrates how SQL can be used to analyze customer behavior and transform raw retail data into meaningful business insights.

It combines data preparation in Python with SQL-based analysis in PostgreSQL, showing a complete workflow from data cleaning to database import and business insight generation.